  # Dependencies
node_modules/

# Build output
dist/
build/
out/

# Environment & secrets
.env
.env.*
!.env.example

# Generated seed data (contains large embedding vectors)
seed-data/*.json

# Lambda packaged zip
/tmp/party-supply-lambda.zip

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db
ehthumbs.db

# IDE / Editor
.idea/
*.swp
*.swo
*~
.vscode/

# Docker
*.pid

# TypeScript incremental compilation
*.tsbuildinfo

# Vite
chat-ui/.vite/

# AWS CDK
cdk.out/
.cdk.staging/
agentcore/cdk/node_modules/
agentcore/cdk/cdk.out/

# AgentCore CLI state (account-specific)
agentcore/.cli/
.agentcore/

# AgentCore aws-targets contains account IDs (use aws-targets.example.json)
agentcore/aws-targets.json

# Coverage & testing
coverage/
.nyc_output/

# Temporary files
tmp/
temp/
*.tmp

# Lambda node_modules (installed at deploy time)
lambda/node_modules/

# Bulk import fixtures generated by scripts/generate-bulk-csv.ts
# (sample-*.csv stays committed; full-size files are regenerable)
uploads/products.csv
uploads/customers.csv
uploads/interactions.csv

# Downsampled temp CSVs written by scripts/batch-import.sh's
# --sample-rate / --since-days path before S3 upload.
uploads/tmp/

# Backup files created during local testing (e.g. interactions-25k.csv.bak)
*.bak
*.csv.bak

# Ad-hoc probe / smoke-test JSON output landing in repo root
/probe.json
/smoke.json
/verify.json
/verify2.json
/chat*.json
/out.json
/final.json
/retry.json
/fix-check.json
/nova.json
/after.json
/claude.json
/baseline.json
/diag.json
/post.json
/ui-followup.json
/followup.json
/initial.json

# Local-only AWS credentials helper (pulls EC2 IMDS creds at runtime)
refresh_aws_credentials.sh

# Claude agent SDK local state
.claude/

# AgentCore runtime config gets rewritten by deploy.sh with account-specific
# resource IDs (memory ID, guardrail ID, account/region). Local-only.
# A baseline agentcore.json.example is committed for new clones.
agentcore/agentcore.json

# Root package-lock - dependency drift varies per environment
package-lock.json
